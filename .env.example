# Advisor Hub Phase 1 — set these in Vercel, never in the repo.
# See db/SETUP.md. Everything degrades quietly when they are absent.

# Supabase. The SERVICE ROLE key bypasses row-level security and is
# server-only — build.js copies js/ verbatim into dist/, so anything in
# client code is public.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# New in V2. Supabase Auth exchanges credentials for a session with this key.
# Project Settings -> API -> anon / public.
SUPABASE_ANON_KEY=

# Used to build absolute links in verification and reset emails.
SITE_ORIGIN=https://discoversaintluciawell.com

# Resend. NOTIFY_FROM must be on a domain verified in Resend, or advisor
# notifications land in spam.
RESEND_API_KEY=
NOTIFY_FROM=

# Any long random string. Used to hash IP addresses for rate limiting so we
# never store an address. Absent = rate limiting skipped, never a weak hash.
IP_HASH_SALT=

# Encharge — advisor onboarding sequences (optional).
# Account → Info → the Ingest API write key.
# Without it api/_lib/encharge.js no-ops and nothing else changes: the Hub,
# registration and Journey notifications all behave exactly as they do now.
# Journey notifications deliberately stay on Resend, so an advisor who
# unsubscribes from onboarding email is still told when a client is waiting.
ENCHARGE_TOKEN=

# ── Campaign generation (optional) ──────────────────────────────────────────
# platform.openai.com/api-keys — a PROJECT key, starting sk-proj-.
#
# This is the API, which is billed separately from a ChatGPT subscription. A
# Plus or Pro plan does NOT include API credit; the account needs its own
# balance under Billing, and it is worth setting a monthly limit there before
# the key is ever used.
#
# Without it everything in My Campaign still works — intake, fact bank, claims
# checker, the copy-paste prompt. Only the generated 30-day plan needs it.
OPENAI_API_KEY=

# ── ThriveCart · the $9 build pack (D7) ──────────────────────────────────────
# Optional. All three fail closed when blank: no secret means /api/hook grants
# nothing, no product id means no purchase is ever identified as the build pack,
# no URL means the "Get 3 more" button does not render.
#
# THRIVECART_SECRET       Settings → API → order validation → "Secret word".
#                         Also append it to the webhook URL as ?k=<same value>.
# THRIVECART_BUILDPACK_ID The $9 pack's product id ONLY. Find it by making a
#                         test purchase and running tools/read-webhook.js —
#                         never by guessing, or a Foundations sale grants builds.
# THRIVECART_BUILDPACK_URL The checkout link the upgrade button points at.
THRIVECART_SECRET=
THRIVECART_BUILDPACK_ID=
THRIVECART_BUILDPACK_URL=

# Foundations, through the SAME ThriveCart account and the same webhook.
# A comma-separated list: 9 is Foundations Standard, 10 is VIP. Both grant the
# identical thing here — unlimited campaigns — because VIP's extra is a strategy
# session that happens off this platform. Add payment-plan ids to the same list.
#
# It does NOT mark anybody as trained. That is foundations_at, set by hand in
# the Hub once they have actually attended, and it is what decides whether their
# copy may say "trained in the Well Destination method". See migration 021.
#
# Blank fails closed: a Foundations order is recorded with a note and grants
# nothing, exactly as an unset pack id behaves.
THRIVECART_FOUNDATIONS_ID=

# ── Where the team hears about things ────────────────────────────────────────
# One address for every notification meant for US rather than for an advisor,
# a traveller or somebody resetting a password. Today that is the Immersion
# waiting list; anything added later reads the same variable.
#
# REQUIRED for those notifications to happen at all. There is no fallback, and
# the absence of one is deliberate: it used to fall back to NOTIFY_FROM, which
# is journeys@ — a sending identity, not a mailbox anybody opens — so the first
# real signup was mailed from the sender to the sender and nobody saw it. Unset
# now means no notification, logged loudly, rather than one delivered nowhere.
# Setting it EQUAL to NOTIFY_FROM is refused for the same reason.
#
# The mailbox has to exist. The domain receives through Purelymail, so this is
# a real inbox there, not an alias that bounces.
ADMIN_EMAIL=
